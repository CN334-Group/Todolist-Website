version: 2.1

jobs:
  test:
    docker:
      - image: circleci/php:8.0.11-node-browsers

    steps:
      - checkout
      
      - run:
          name: "Run Unit Tests"
          command: ./vendor/bin/phpunit tests/Unit

workflows:
  test-workflow:
    jobs:
      - test